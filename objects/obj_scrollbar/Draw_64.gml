//Scrollbar Umrandung
draw_rectangle(x, y, x + width, y + heigth, 1);
//Scrollbar
draw_rectangle(	x + spacing + 1,
				y + spacing + 1, 
				x + width - (spacing + 1), 
				y + heigth - (spacing + 1), 0);

